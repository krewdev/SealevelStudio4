'use client';

import { UnifiedWalletControl } from './UnifiedWalletControl';

/** @deprecated Use UnifiedWalletControl. Kept so old imports still hit one signer. */
export const WalletButton = () => <UnifiedWalletControl />;

export default WalletButton;
